# UY-NB-Meter 开发文档

## 1. 项目概述

`UY-NB-Meter` 是一个基于 Flask 的单文件模拟水表 API 服务，目标是给产品、设备、后端和测试同事提供一个稳定、可观察、可快速修改的联调环境。

当前版本核心能力：

- 接收 `POST /HMWSSBAPI/PostMeterReadingData`
- 支持 AES-128-GCM 请求解密与响应加密
- 保留宽松鉴权规则，方便测试
- 将最近请求记录保存到内存并展示在首页

项目结构保持不变：

- Flask
- 单文件 `server.py`
- `render_template_string` 内联 HTML
- 内存列表 `HISTORY_DATA`

## 2. 核心配置

主要常量：

- `EXPECTED_BODY_LENGTH = 158`
- `MAX_HISTORY = 50`
- `VALID_AUTH_KEYS`
- `DEFAULT_AES_GCM_KEY_HEX = b8286d10dc8ae670189223a299b0affb`
- `DEFAULT_AES_GCM_RESPONSE_KEY_HEX = 45e036e26c95279ee61c8f452ee35543`

主要环境变量：

- `AES_GCM_ENABLED`
- `AES_GCM_KEY`
- `AES_GCM_RESPONSE_KEY`
- `AES_GCM_REQUEST_AAD`
- `AES_GCM_RESPONSE_AAD`
- `AES_GCM_KEY_STORE_PATH`
- `AES_GCM_ACTIVE_KEY_NAME`

字节类环境变量支持以下格式：

- `hex:...`
- `base64:...`
- `text:...`
- 不带前缀时按普通文本编码为 UTF-8 bytes

## 3. 对外接口

### 3.1 首页

- 路径：`/`
- 方法：`GET`
- 作用：展示 AES 模式、key 信息、测试使用说明和请求日志

### 3.2 清空历史

- 路径：`/clear`
- 方法：`GET`
- 作用：清空内存中的请求历史

### 3.3 上报接口

- 路径：`/HMWSSBAPI/PostMeterReadingData`
- 方法：`POST`
- Content-Type：不强制

### 3.4 Key 管理接口

- `POST /keys/generate`
- `POST /keys/activate`
- `POST /aes-mode`
- `POST /response-encryption`

说明：

- 这两个接口主要供开发测试切换 key 使用
- `POST /aes-mode` 与 `POST /response-encryption` 主要供首页按钮切换运行时模式
- 当前沿用和上报接口相同的宽松鉴权规则

## 4. 鉴权逻辑

白名单定义在 `VALID_AUTH_KEYS`：

- `Basic ZWRwOk5hdmF5dWdhMTIz`
- `Basic YWRtaW46MTIzNDU2`

统一规则如下：

1. 没有 `Authorization` 请求头时，视为通过
2. 有 `Authorization` 请求头且值命中白名单时，视为通过
3. 只有“带了请求头但不在白名单”时才失败

失败时返回：

- HTTP `401`
- 响应体：`faile`

## 5. AES 与请求体处理逻辑

### 5.1 AES 关闭时

- 请求体按明文处理
- 响应体返回明文 `OK` / `faile`

### 5.2 AES 开启时

- 请求体必须为 `base64(nonce + ciphertext + tag)`
- `nonce` 长度必须是 12 bytes
- 使用 `AESGCM` 解密
- 解密后的文本按 UTF-8 解码
- 解密后的文本必须是 hex 文本

### 5.2.1 首页两个模式按钮

首页 `Key Management` 面板包含两个独立的运行时按钮组：

- `AES-GCM Mode`
- `Response Format`

它们分别控制：

- `AES-GCM Mode`：是否对请求体执行 AES-GCM 解密，对应 `app.config["AES_GCM_ENABLED"]`
- `Response Format`：是否对响应体执行 AES-GCM 加密，对应 `app.config["AES_GCM_ENCRYPT_RESPONSE"]`

典型组合：

- `Enabled + Encrypted`：最接近真实协议联调
- `Enabled + Plaintext`：排查请求解密时最方便
- `Disabled + Plaintext`：手工构造明文 hex 请求最方便
- `Disabled + Encrypted`：较少使用，主要用于单独验证响应加密输出

### 5.3 协议包校验

当前版本不解析业务字段，只校验协议包长度。

处理顺序：

1. 读取原始请求体
2. 如启用 AES，则先做 base64 解码和 AES-GCM 解密
3. 对解密后的文本做 `strip`
4. 删除其中所有空白符
5. 检查是否全部为 hex 字符
6. 检查字符数是否为偶数
7. `bytes.fromhex(...)`
8. 检查字节长度是否等于 `158`

只有最后一步通过才返回成功。

## 6. 错误分类

首页会展示详细失败原因，常见类型包括：

- `Auth Failed`
- `Decrypt Failed`
- `Empty Body`
- `Not Hex Text`
- `Odd Hex Length`
- `Hex Decode Failed`
- `Protocol Length Failed`

页面中会同时展示：

- 原始请求体长度
- 解密后文本长度
- hex 文本长度
- 协议字节长度
- 请求解密状态
- 响应加密状态

## 7. 历史记录结构

每次请求都会写入一条内存记录，主要字段包括：

- `Success`
- `AuthPassed`
- `LengthPassed`
- `RawBodyTextLength`
- `DecryptedTextLength`
- `HexTextLength`
- `ProtocolByteLength`
- `FieldCount`
- `RequestCryptoStatus`
- `ResponseCryptoStatus`
- `HexDecodeStatus`
- `RawData`
- `ErrorType`
- `ErrorMsg`
- `Time`
- `Pkey`

补充说明：

- `FieldCount` 只作为调试辅助字段，不参与通过/失败判定
- `RawData` 是请求体按 UTF-8 `replace` 转换后的显示值
- 页面中的 `Auth` 会显示为 `PASS (missing)`、`PASS` 或 `FAIL`

## 8. 首页逻辑

首页当前分为三部分：

1. 顶部版本和接口说明
2. `Key Management` 与 `Test Usage Guide`
3. 请求日志表

页面主要面向联调使用，因此优先展示：

- 当前是否启用 AES
- 当前使用的请求 key / 响应 key
- key 来源是环境变量还是内置测试 key
- 测试方法和失败原因解释

## 9. 运行与测试

推荐使用虚拟环境：

```bash
cd /home/yobai/UY-NB-Meter
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python server.py
```

运行单元测试：

```bash
.venv/bin/python -m unittest -v
```

## 10. 当前限制

- `HISTORY_DATA` 只保存在内存中，进程重启后会丢失
- 页面模板仍内嵌在 `server.py`
- 没有数据库
- 没有独立日志落盘
- 当前 `debug=True`，更适合开发测试，不适合长期生产

## 11. 开发注意事项

- 不要引入新的框架
- 不要拆分成多文件架构
- 不要增加模板目录
- 不要接入数据库
- 如果继续扩展，优先保持 `server.py` 可读性和最小改动原则
- 若继续强化 AES 功能，优先补测试和文档，再扩展新行为
