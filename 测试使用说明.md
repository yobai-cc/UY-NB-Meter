# UY-NB-Meter 测试使用说明

本文档给产品、联调和验收同事使用。当前接口默认测试目标不是“直接发送 158 个字符”，而是“发送 AES-128-GCM 加密后的 base64 文本”。

## 1. 测试地址

### 1.1 网页地址

- `https://uynbapi.bovetech.cn:15001/`

说明：

- 这是监控页面和测试说明页面
- 可以直接用浏览器打开
- 页面会显示 key、协议说明、失败原因和最近请求记录

### 1.2 API 地址

- `http://uynbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData`

说明：

- 该地址用于设备或测试工具上报数据
- 只支持 `POST`
- 直接用浏览器打开没有意义

## 2. 先做网页检查

先访问：

- `https://uynbapi.bovetech.cn:15001/`

如果正常，应该看到：

- 页面能打开
- 页面有 `Key Management`
- 页面有 `Test Usage Guide`
- 页面有请求日志表

如果打不开，请记录：

- 打开时间
- 浏览器报错内容
- 是否超时、证书报错、404、502、空白页
- 最好附上截图

## 3. API 测试前说明

接口只支持 `POST`。

如果你用浏览器直接打开 API 地址，或者用 `GET` 请求访问，返回 `405 Method Not Allowed` 是正常现象。

当前接口在 AES 模式下要求：

- 请求体是 base64 文本
- base64 解码后内容是 `12-byte nonce + AES-GCM ciphertext + tag`
- 解密后明文是 hex 文本
- hex 解码后的字节长度必须等于 `158`

## 4. 固定测试 key

当前测试环境默认使用：

- 请求解密 key：`b8286d10dc8ae670189223a299b0affb`
- 响应加密 key：`45e036e26c95279ee61c8f452ee35543`

说明：

- 这两把 key 仅供开发测试使用
- 如果服务端改成了环境变量 key，请以网页显示的 key 为准

## 5. 标准成功测试

### 5.1 生成合法请求体

在 Linux / macOS / WSL 终端执行：

```bash
BODY="$(python3 - <<'PY'
import base64
from cryptography.hazmat.primitives.ciphers.aead import AESGCM

key = bytes.fromhex("b8286d10dc8ae670189223a299b0affb")
nonce = b"UYNBMETER123"
plain_text = "AA" * 158

payload = AESGCM(key).encrypt(nonce, plain_text.encode("utf-8"), None)
print(base64.b64encode(nonce + payload).decode("ascii"))
PY
)"
```

### 5.2 发送成功请求

```bash
curl -i -X POST "http://uynbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData" \
  -H "Authorization: Basic ZWRwOk5hdmF5dWdhMTIz" \
  -H "Content-Type: text/plain" \
  --data-binary "$BODY"
```

### 5.3 预期成功结果

正常情况下应返回：

- HTTP 状态码：`200 OK`
- 返回内容：一段 base64 文本

说明：

- 当前响应在 AES 模式下也会被加密
- 所以你看到的不是明文 `OK`

同时网页中应新增一条成功记录，通常表现为：

- `Result`: `200 OK`
- `Auth`: `PASS`
- `Request Crypto`: `Decrypted`
- `Response Crypto`: `Encrypted`
- `Protocol Byte Length`: `158`

## 6. 不带 Authorization 的成功测试

当前系统保留宽松鉴权规则，因此也可以不带 `Authorization` 测试：

```bash
curl -i -X POST "http://uynbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData" \
  -H "Content-Type: text/plain" \
  --data-binary "$BODY"
```

预期结果：

- HTTP 状态码：`200 OK`
- 页面中 `Auth` 显示 `PASS (missing)`

## 7. 鉴权失败测试

可以用下面命令测试错误鉴权：

```bash
curl -i -X POST "http://uynbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData" \
  -H "Authorization: Basic invalid" \
  -H "Content-Type: text/plain" \
  --data-binary "$BODY"
```

预期结果：

- HTTP 状态码：`401`
- 返回内容：一段 base64 文本

网页里会看到一条失败记录，通常表现为：

- `Auth`: `FAIL`
- `Error`: `Auth Failed`

## 8. AES 请求错误测试

可以用下面命令测试明显错误的请求体：

```bash
curl -i -X POST "http://uynbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData" \
  -H "Authorization: Basic ZWRwOk5hdmF5dWdhMTIz" \
  -H "Content-Type: text/plain" \
  --data-binary "not-base64"
```

预期结果：

- HTTP 状态码：`400`
- 返回内容：一段 base64 文本

网页里会看到一条失败记录，通常表现为：

- `Request Crypto`: `Decrypt failed`
- `Error`: `Decrypt Failed`

## 9. 长度错误测试

可以构造一个解密成功但协议长度不为 `158` 的请求：

```bash
BAD_BODY="$(python3 - <<'PY'
import base64
from cryptography.hazmat.primitives.ciphers.aead import AESGCM

key = bytes.fromhex("b8286d10dc8ae670189223a299b0affb")
nonce = b"UYNBMETER124"
plain_text = "AA" * 157

payload = AESGCM(key).encrypt(nonce, plain_text.encode("utf-8"), None)
print(base64.b64encode(nonce + payload).decode("ascii"))
PY
)"
```

```bash
curl -i -X POST "http://uynbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData" \
  -H "Authorization: Basic ZWRwOk5hdmF5dWdhMTIz" \
  -H "Content-Type: text/plain" \
  --data-binary "$BAD_BODY"
```

预期结果：

- HTTP 状态码：`400`
- 返回内容：一段 base64 文本

网页里会看到一条失败记录，通常表现为：

- `Hex Decode Status`: `OK`
- `Protocol Byte Length`: `157`
- `Error`: `Protocol Length Failed`

## 10. Postman 测试方法

如果不方便使用命令行，也可以用 Postman。

请求设置：

- Method：`POST`
- URL：`http://uynbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData`
- Header：
  - `Content-Type: text/plain`
  - `Authorization: Basic ZWRwOk5hdmF5dWdhMTIz` 或不填写
- Body：选择 `raw / Text`

注意事项：

- 请求体不能直接填 `158` 个 `A`
- 请求体必须填“加密后的 base64 文本”
- 可以先用上面的 Python 脚本生成，再粘贴到 Postman

## 11. 常见现象说明

### 11.1 网页能打开，说明什么？

- HTTPS 页面可访问
- 反向代理基本正常
- 后端页面服务基本正常

### 11.2 API 返回 405 是不是故障？

不是，通常表示你用了 `GET`，而接口只允许 `POST`。

### 11.3 为什么返回的不是明文 `OK`？

因为当前 AES 模式开启后，响应也会被 AES-GCM 加密，所以看到的是 base64 密文。

### 11.4 页面里的错误说明看哪里？

重点看：

- `Request Crypto`
- `Response Crypto`
- `Decode Status`
- `Error`
- `Protocol Byte Length`

## 12. 同事反馈问题时请提供的信息

如果测试失败，请反馈：

1. 测试时间
2. 使用的是网页还是 API
3. 访问的完整地址
4. 使用的是浏览器、curl 还是 Postman
5. 完整返回结果或截图
6. 如果是 curl，请附完整命令和完整输出

建议按下面模板反馈：

```text
测试时间：
测试方式：浏览器 / curl / Postman
测试地址：
请求方法：GET / POST
是否带 Authorization：
返回状态码：
返回内容：
页面 Error 字段：
现象描述：
截图：有 / 无
```
