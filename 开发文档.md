# UY-NB-Meter 开发文档

## 1. 项目概述

`UY-NB-Meter` 是一个基于 Flask 的单文件模拟水表 API 服务，目标是给产品、设备、后端和测试同事提供一个稳定、可观察、可快速修改的联调环境。

当前版本核心能力：

- 接收 `POST /HMWSSBAPI/PostMeterReadingData`
- 支持 AES-128-GCM 请求解密与响应加密
- 支持 JSON 响应格式（`ResponseCode` / `ResponseType` / `Description`），兼容固件协议
- 支持下行 hex 数据动态配置，成功时随响应下发
- 保留宽松鉴权规则，方便测试
- 将最近请求记录保存到内存并展示在首页

项目结构保持不变：

- Flask
- 单文件 `server.py`
- `render_template_string` 内联 HTML
- 内存列表 `HISTORY_DATA`

## 2. 核心配置

主要常量：

- `EXPECTED_BODY_LENGTH = 158`
- `MAX_HISTORY = 50`
- `VALID_AUTH_KEYS`
- `DEFAULT_AES_GCM_KEY_HEX = b8286d10dc8ae670189223a299b0affb`
- `DEFAULT_AES_GCM_RESPONSE_KEY_HEX = 45e036e26c95279ee61c8f452ee35543`
- `DEFAULT_DOWNLINK_HEX = FE FE 68 10 AA AA AA AA AA AA AA 25 04 30 05 00 00 7C 16`

主要环境变量：

- `AES_GCM_ENABLED`
- `AES_GCM_KEY`
- `AES_GCM_RESPONSE_KEY`
- `AES_GCM_REQUEST_AAD`
- `AES_GCM_RESPONSE_AAD`
- `AES_GCM_KEY_STORE_PATH`
- `AES_GCM_ACTIVE_KEY_NAME`
- `RESPONSE_FORMAT`：响应格式，取值 `plaintext`（默认）或 `json`
- `DOWNLINK_HEX`：成功时携带的下行 hex 数据，支持带空格，默认使用 `DEFAULT_DOWNLINK_HEX`

字节类环境变量支持以下格式：

- `hex:...`
- `base64:...`
- `text:...`
- 不带前缀时按普通文本编码为 UTF-8 bytes

## 3. 对外接口

### 3.1 首页

- 路径：`/`
- 方法：`GET`
- 作用：展示 AES 模式、key 信息、测试使用说明和请求日志

### 3.2 清空历史

- 路径：`/clear`
- 方法：`GET`
- 作用：清空内存中的请求历史

### 3.3 上报接口

- 路径：`/HMWSSBAPI/PostMeterReadingData`
- 方法：`POST`
- Content-Type：不强制

### 3.4 Key 管理接口

- `POST /keys/generate`
- `POST /keys/activate`
- `POST /aes-mode`
- `POST /response-encryption`
- `POST /response-format`：切换响应格式（plaintext / json）
- `POST /downlink`：设置下行 hex 数据

说明：

- `POST /keys/generate` 与 `POST /keys/activate` 主要供开发测试切换 key 使用
- `POST /aes-mode` 与 `POST /response-encryption` 主要供首页按钮切换运行时模式
- `POST /response-format` 与 `POST /downlink` 是新增的运行时控制接口，同样通过首页按钮触发
- 当前沿用和上报接口相同的宽松鉴权规则

## 4. 鉴权逻辑

白名单定义在 `VALID_AUTH_KEYS`：

- `Basic ZWRwOk5hdmF5dWdhMTIz`
- `Basic YWRtaW46MTIzNDU2`

统一规则如下：

1. 没有 `Authorization` 请求头时，视为通过
2. 有 `Authorization` 请求头且值命中白名单时，视为通过
3. 只有“带了请求头但不在白名单”时才失败

失败时返回：

- HTTP `401`
- 响应体：`faile`

## 5. AES 与请求体处理逻辑

### 5.1 AES 关闭时

- 请求体按明文处理
- 响应体返回明文 `OK` / `faile`

### 5.2 AES 开启时

- 请求体必须为 `base64(nonce + ciphertext + tag)`
- `nonce` 长度必须是 12 bytes
- 使用 `AESGCM` 解密
- 解密后的文本按 UTF-8 解码
- 解密后的文本必须是 hex 文本

### 5.2.1 首页模式按钮

首页 `Key Management` 面板包含四个运行时按钮组：

- `AES-GCM Mode`
- `Response Encryption`
- `Response Format`
- `Downlink Hex`

它们分别控制：

- `AES-GCM Mode`：是否对请求体执行 AES-GCM 解密，对应 `app.config["AES_GCM_ENABLED"]`
- `Response Encryption`：是否对响应体执行 AES-GCM 加密，对应 `app.config["AES_GCM_ENCRYPT_RESPONSE"]`
- `Response Format`：响应体结构（`plaintext` / `json`），对应对 `app.config["RESPONSE_FORMAT"]`
- `Downlink Hex`：成功时随响应下发的下行 hex 数据，对应 `app.config["DOWNLINK_HEX"]`

说明：`RESPONSE_FORMAT` 和 `DOWNLINK_HEX` 是运行时动态配置，仅存于内存；切换后立即生效但进程重启会丢失。

### 5.2.2 JSON 响应格式

当 `RESPONSE_FORMAT = json` 时，所有响应均返回统一 JSON 结构：

```json
{
  "m_Item1": {
    "ResponseCode": "200",
    "ResponseType": "Sucess",
    "Description": "<当前下行 hex>"
  }
}
```

- HTTP 状态码始终为 `200`，真实状态码在 `ResponseCode` 字段中
- `"Sucess"` 是下游设备固件的固定协议字段名（单 `s`），不可修改
- `Description` 字段在成功时携带当前下行 hex，失败时为错误类型名
- JSON 模式下 `Response Encryption = Encrypted` 时，整个 JSON 字符串会作为 AES-GCM 明文加密返回 base64

典型组合：

- `Enabled + Encrypted + json`：最接近真实协议联调（含下行数据）
- `Enabled + Plaintext + plaintext`：排查请求解密时最方便
- `Disabled + Plaintext + plaintext`：手工构造明文 hex 请求最方便
- `Disabled + Encrypted + json`：单独验证 JSON 响应加密输出

### 5.2.3 下行 Hex 数据

成功响应时会携带下行 hex 数据（`Description` 字段，或暂不体现在 plaintext 响应体中）。

- 存储在 `DOWNLINK_HEX` 配置中，支持带空格格式（如 `FE FE 68 10 ...`），方便在首页阅读和编辑
- 首页 `Downlink Hex` 输入框可直接修改，提交 `POST /downlink` 即可更新
- 校验时会自动删除空白符再解析，不影响协议字节比对
- 输入限长 1024 hex 字符（不含空格）

### 5.2.4 响应加密静默回退

如果 AES 响应加密失败（如 key 未配置、AAD 格式错误等），服务端会**静默回退到明文响应**，不会直接报 500 错误。

- 回退后 HTTP 状态码和响应 body 与不加密模式完全一致
- 首页日志中的 `Response Crypto` 列会显示为 `Plaintext (Encrypt Failed)`，提示加密失败

### 5.3 协议包校验

当前版本不解析业务字段，只校验协议包长度。

处理顺序：

1. 读取原始请求体
2. 如启用 AES，则先做 base64 解码和 AES-GCM 解密
3. 对解密后的文本做 `strip`
4. 删除其中所有空白符
5. 检查是否全部为 hex 字符
6. 检查字符数是否为偶数
7. `bytes.fromhex(...)`
8. 检查字节长度是否等于 `158`

只有最后一步通过才返回成功。

## 6. 错误分类

首页会展示详细失败原因，常见类型包括：

- `Auth Failed`
- `Decrypt Failed`
- `Empty Body`
- `Not Hex Text`
- `Odd Hex Length`
- `Hex Decode Failed`
- `Protocol Length Failed`
- `Encrypt Failed`：响应加密失败（静默回退）

页面中会同时展示：

- 原始请求体长度
- 解密后文本长度
- hex 文本长度
- 协议字节长度
- 请求解密状态
- 响应加密状态（失败时显示 `Plaintext (Encrypt Failed)`）
- 响应数据预览（`Response Data` 列）

## 7. 历史记录结构

每次请求都会写入一条内存记录，主要字段包括：

- `Success`
- `AuthPassed`
- `LengthPassed`
- `RawBodyTextLength`
- `DecryptedTextLength`
- `HexTextLength`
- `ProtocolByteLength`
- `FieldCount`
- `RequestCryptoStatus`
- `ResponseCryptoStatus`
- `HexDecodeStatus`
- `RawData`
- `ErrorType`
- `ErrorMsg`
- `Time`
- `Pkey`
- `ResponseData`：实际返回的响应体预览（JSON 模式下为完整 JSON 字符串，plaintext 模式下为 `OK` / `faile` / 加密后的 base64）

补充说明：

- `FieldCount` 只作为调试辅助字段，不参与通过/失败判定
- `RawData` 是请求体按 UTF-8 `replace` 转换后的显示值
- 页面中的 `Auth` 会显示为 `PASS (missing)`、`PASS` 或 `FAIL`

## 8. 首页逻辑

首页当前分为四部分：

1. 顶部版本和接口说明
2. `Key Management`（含 AES 模式、响应加密、响应格式、下行数据四个面板）
3. `Test Usage Guide`
4. 请求日志表

页面主要面向联调使用，因此优先展示：

- 当前是否启用 AES
- 当前是否加密响应
- 当前响应格式（plaintext / json）
- 当前下行 hex 数据
- 当前使用的请求 key / 响应 key
- key 来源是环境变量还是内置测试 key
- 测试方法和失败原因解释

## 9. 运行与测试

推荐使用虚拟环境：

```bash
cd /home/yobai/UY-NB-Meter
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python server.py
```

运行单元测试：

```bash
.venv/bin/python -m unittest -v
```

### 9.1 远程部署（一键脚本）

`install.sh` 内置 curl 超时保护（建连 10s / 传输 120s），大陆网络不佳时不会无限卡死。

安装：

```bash
curl -fsSL https://raw.githubusercontent.com/yobai-cc/UY-NB-Meter/main/install.sh | TARGET_DIR=/opt/UY-NB-Meter PIP_REGION=mainland PIP_TIMEOUT=20 PIP_RETRIES=2 PIP_RESUME_RETRIES=2 INSTALL_SERVICE=1 bash -s -- install
```

更新（指定版本）：

```bash
curl -fsSL https://raw.githubusercontent.com/yobai-cc/UY-NB-Meter/main/install.sh | TARGET_DIR=/opt/UY-NB-Meter RELEASE_TAG=v2026.04.29 PIP_REGION=mainland PIP_TIMEOUT=20 PIP_RETRIES=2 PIP_RESUME_RETRIES=2 bash -s -- update
```

大陆必须指定 `PIP_REGION=mainland` 走清华镜像，否则 pip install 会超时。`PIP_TIMEOUT` / `PIP_RETRIES` 设低可以快速失败并走回退源，不必用默认值。

## 10. 当前限制

- `HISTORY_DATA` 只保存在内存中，进程重启后会丢失
- 页面模板仍内嵌在 `server.py`
- 没有数据库
- 没有独立日志落盘
- 当前 `debug=True`，更适合开发测试，不适合长期生产

## 11. 开发注意事项

- 不要引入新的框架
- 不要拆分成多文件架构
- 不要增加模板目录
- 不要接入数据库
- 如果继续扩展，优先保持 `server.py` 可读性和最小改动原则
- 若继续强化 AES 功能，优先补测试和文档，再扩展新行为

## 12. 已知注意事项与陷阱

### 12.1 "Sucess" 拼写说明

JSON 响应中的 `"Sucess"`（单 s）不是拼写错误，而是下游 NB-IoT 设备固件的固定协议字段名。将其改为 `"Success"` 会导致设备解析响应失败。代码中已加注释提醒，切勿 "纠正"。

### 12.2 下行 Hex 的空格存储

`DOWNLINK_HEX` 按联调习惯**带空格存储**（如 `FE FE 68 10 ...`），方便在首页阅读和编辑。但在校验和用于响应构建时会自动删除所有空白符。因此首页看到的 hex 和实际用于协议比对的 hex 格式不同——这是有意为之。

### 12.3 动态配置不持久

`RESPONSE_FORMAT`、`DOWNLINK_HEX` 以及 `AES_GCM_ENABLED`、`AES_GCM_ENCRYPT_RESPONSE` 通过首页按钮修改后**只影响当前进程**，不会写回 `.env`。进程重启后，这些值会从环境变量或代码默认值重新初始化。如需持久化，请写入 `.env` 文件。

### 12.4 响应加密静默降级

AES 响应加密失败时，服务端不会返回 5xx 错误，而是静默回退到明文响应。客户端收到的仍然是正常的 HTTP 200 + 明文 body（不加密），但首页日志会显示 `Plaintext (Encrypt Failed)` 以提示开发人员。

### 12.5 HTML 模板嵌入在 Python 中

首页 HTML 直接写在 `HTML_TEMPLATE` 字符串常量中（约 200 行），不引入 `templates/` 目录。这是单文件架构的刻意要求。修改 UI 时直接改此字符串，重启服务生效。

### 12.6 请求历史不持久

`HISTORY_DATA` 仅存于内存，进程重启后丢失。如需长期保存联调历史，建议通过外部脚本轮询首页日志或从网关层面记录请求。

### 12.7 install.sh 大陆使用时需显式指定 PIP_REGION

`install.sh` 下载 Release 包时已内置 curl 超时（10s 建连 / 120s 传输），不会再因 GitHub 连不上而无限挂起。但 `deploy/deploy_update.sh` 的 pip 安装步骤默认走 `overseas`（pypi.org），大陆服务器必须通过环境变量显式设置：

```bash
PIP_REGION=mainland PIP_TIMEOUT=20 PIP_RETRIES=2
```

否则 pip install 会超时卡死。推荐直接写进 `.env` 持久化。
