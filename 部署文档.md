# UY-NB-Meter 部署文档

## 1. 文档目标

本文档用于说明 `UY-NB-Meter` 在 Windows 或通用服务器环境中的部署方式。当前服务是单文件 Flask 应用，支持 AES-128-GCM 请求解密与响应加密。

已知对外地址：

- HTTPS 页面：`https://hmwssbapi.bovetech.cn:15001/`
- HTTP API：`http://hmwssbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData`

本地监听地址：

- `127.0.0.1:15556`

## 2. 推荐部署结构

```text
设备 / 浏览器
        |
        v
反向代理（Caddy）
        |
        v
127.0.0.1:15556
        |
        v
Flask server.py
```

说明：

- Flask 负责接口处理和页面展示
- 反向代理负责端口暴露、域名绑定和 HTTPS
- 页面和 API 都转发到同一个 Flask 进程

## 3. 部署前准备

建议环境：

- Python 3.12
- 可用域名解析
- 可运行反向代理

依赖安装：

```powershell
cd D:\apps\UY-NB-Meter
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

当前依赖：

- `Flask==3.0.3`
- `cryptography==45.0.7`

## 4. AES 配置

当前服务支持两种方式提供 AES key：

1. 使用代码内置测试 key
2. 使用环境变量覆盖

内置测试 key：

- 请求解密 key：`hex:b8286d10dc8ae670189223a299b0affb`
- 响应加密 key：`hex:45e036e26c95279ee61c8f452ee35543`

建议部署时显式配置：

```powershell
set AES_GCM_ENABLED=true
set AES_GCM_ENCRYPT_RESPONSE=true
set AES_GCM_KEY=hex:b8286d10dc8ae670189223a299b0affb
set AES_GCM_RESPONSE_KEY=hex:45e036e26c95279ee61c8f452ee35543
```

可选环境变量：

- `AES_GCM_ENCRYPT_RESPONSE`
- `AES_GCM_REQUEST_AAD`
- `AES_GCM_RESPONSE_AAD`
- `AES_GCM_KEY_STORE_PATH`
- `AES_GCM_ACTIVE_KEY_NAME`

## 5. 启动方式

直接启动：

```powershell
cd D:\apps\UY-NB-Meter
.\venv\Scripts\activate
set AES_GCM_ENABLED=true
set AES_GCM_ENCRYPT_RESPONSE=true
.\venv\Scripts\python.exe .\server.py
```

启动后服务监听：

- `127.0.0.1:15556`

本机访问：

- `http://127.0.0.1:15556/`
- `http://127.0.0.1:15556/HMWSSBAPI/PostMeterReadingData`

## 6. Caddy 配置示例

```caddy
https://hmwssbapi.bovetech.cn:15001 {
    reverse_proxy 127.0.0.1:15556
}

http://hmwssbapi.bovetech.cn:15000 {
    handle /HMWSSBAPI* {
        reverse_proxy 127.0.0.1:15556
    }

    handle {
        redir https://hmwssbapi.bovetech.cn:15001{uri}
    }
}
```

## 7. 首次部署建议

建议顺序：

1. 将项目放到正式目录，例如 `D:\apps\UY-NB-Meter`
2. 创建并激活虚拟环境
3. 安装依赖 `pip install -r requirements.txt`
4. 配置 `AES_GCM_ENABLED` 和 key
5. 本机启动 `server.py`
6. 访问 `http://127.0.0.1:15556/` 确认页面正常
7. 配置 Caddy
8. 配置域名与防火墙
9. 访问 HTTPS 页面验证
10. 发送一次 AES 测试请求验证 API

## 8. 接口验证

### 8.1 页面验证

浏览器访问：

- `http://127.0.0.1:15556/`
- `https://hmwssbapi.bovetech.cn:15001/`

应能看到：

- `Key Management`
- `Test Usage Guide`
- 请求日志表

### 8.2 API 验证

先生成请求体：

```powershell
python -c "import base64; from cryptography.hazmat.primitives.ciphers.aead import AESGCM; key=bytes.fromhex('b8286d10dc8ae670189223a299b0affb'); nonce=b'UYNBMETER123'; plain='AA'*158; payload=AESGCM(key).encrypt(nonce, plain.encode('utf-8'), None); print(base64.b64encode(nonce+payload).decode('ascii'))"
```

将输出的 base64 文本作为请求体发送：

```powershell
curl.exe -X POST "http://hmwssbapi.bovetech.cn:15000/HMWSSBAPI/PostMeterReadingData" `
  -H "Authorization: Basic ZWRwOk5hdmF5dWdhMTIz" `
  -H "Content-Type: text/plain" `
  --data-binary "这里替换成生成的 base64"
```

成功时预期返回：

- HTTP `200`
- 响应体为一段 base64 文本

## 9. 更新部署

更新步骤建议：

1. 备份当前项目目录
2. 替换项目文件
3. 如依赖变化，重新执行 `pip install -r requirements.txt`
4. 重新确认 AES 环境变量
5. 重启服务
6. 验证页面与 API

## 10. 常见问题排查

### 10.1 页面打不开

检查：

- Flask 是否启动成功
- `127.0.0.1:15556` 是否本机可访问
- 反向代理是否启动
- 域名解析是否正确
- 防火墙是否放行 `15000` 和 `15001`

### 10.2 接口返回 401

检查：

- 是否带了错误的 `Authorization`
- 白名单是否仍是：
  - `Basic ZWRwOk5hdmF5dWdhMTIz`
  - `Basic YWRtaW46MTIzNDU2`

说明：

- 不带 `Authorization` 时当前代码会放行
- 带了错误值时才会返回 `401`

### 10.3 接口返回 400

重点检查：

- 请求体是否为 base64 文本
- AES key 是否与服务端一致
- nonce / ciphertext / tag 是否完整
- 解密后是否为 hex 文本
- 解码后是否正好是 `158` bytes

### 10.4 返回内容为什么不是明文 `OK`

如果启用了 `AES_GCM_ENCRYPT_RESPONSE=true`，响应体会被 AES-GCM 加密，因此看到的是 base64 密文，这是正常现象。

## 11. 生产建议

当前项目更偏向开发测试，不建议直接长期生产化使用。若后续用于更严肃环境，建议至少补齐：

- 将 `debug=True` 改为 `debug=False`
- 将白名单改为环境变量配置
- 增加落盘日志
- 增加历史持久化
- 视需要收紧“不带 Authorization 也放行”的规则
